{% extends 'base.html.twig' %}

{% block title %}Détails de la Formation{% endblock %}

{% block body %}
    <div class="container py-4">
        <h1 class="mb-4">📘 Détails de la Formation</h1>

        <div class="card shadow-sm">
            <div class="card-body">
                <table class="table table-bordered mb-0">
                    <tbody>
                    <tr>
                        <th scope="row">ID</th>
                        <td>{{ formation.id }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Titre</th>
                        <td>{{ formation.title }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Description</th>
                        <td>{{ formation.description }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Prix</th>
                        <td>{{ formation.price }} €</td>
                    </tr>
                    <tr>
                        <th scope="row">Publié</th>
                        <td>{{ formation.published ? 'Oui' : 'Non' }}</td>
                    </tr>
                    </tbody>
                </table>
            </div>
        </div>

        <div class="mt-4 d-flex gap-2">
            <a href="{{ path('app_formation_index') }}" class="btn btn-secondary">← Retour à la liste</a>
            <a href="{{ path('app_formation_edit', {'id': formation.id}) }}" class="btn btn-primary">✏️ Modifier</a>
            {{ include('formation/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
